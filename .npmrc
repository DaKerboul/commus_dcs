registry=https://registry.npmjs.org

# Docker builds run npm ci twice (builder + runner stages), so a single flaky
# registry read fails the whole deploy. Retry transient network errors instead.
fetch-retries=5
fetch-retry-mintimeout=2000
fetch-retry-maxtimeout=60000
fetch-timeout=300000
